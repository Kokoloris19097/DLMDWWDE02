apiVersion: v1
kind: Service
metadata:
  name: {{ include "system-cluster.fullname" . }}
  labels:
    {{- include "system-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "system-cluster.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "system.fullname" . }}-test-connection"
  labels:
    {{- include "system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
